suite: iperf-walk-os-test
category: benchmark
runtime: 300
cluster: cs-localhost
if role server:
  iperf-server:
if role client:
  iperf:
    protocol: tcp
node_roles: server client
LKP_DEBUG_PREFIX: bash -x
